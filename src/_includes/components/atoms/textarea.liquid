<textarea 
    name="{{ name }}"
    class="textarea"
    {% if id %} id="{{ id }}" {% endif %}
    {% if required %} required="{{ required }}" {% endif %}
    {% if spellcheck %} spellcheck="{{ spellcheck }}" {% endif %}
></textarea>