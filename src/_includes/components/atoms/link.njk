{% macro link(href, text, rel="", inverted=false) %}
    <a href="{{ href }}" class="link {% if inverted %}link--inverted{% endif %}" {% if rel %} rel="{{ rel }}" {% endif %}>{{ text | safe }}</a>
{% endmacro %}