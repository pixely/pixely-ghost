<input
    name="{{ name }}"
    class="input"
    type="{{ type }}"
    {% if id %} id="{{ id }}" {% endif %}
    {% if value %} value="{{ value }}" {% endif %}
    {% if required %} required="{{ required }}" {% endif %}
/>